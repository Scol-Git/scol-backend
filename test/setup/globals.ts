/**
 * Global Jest Setup
 * 
 * This file runs before all tests.
 * Use it for global test configuration, mocks, or setup.
 * 
 * To use: Add to jest.config.js:
 * {
 *   "setupFilesAfterEnv": ["<rootDir>/test/setup/globals.ts"]
 * }
 */

// Example: Mock console methods to reduce noise in tests
// global.console = {
//   ...console,
//   log: jest.fn(),
//   debug: jest.fn(),
//   info: jest.fn(),
//   warn: jest.fn(),
//   error: jest.fn(),
// };

