/**
 * Generic pointer for what the activity row is primarily about (varchar in DB).
 */
export enum ApplicationActivityEntityType {
  Application = 'APPLICATION',
  ApplicationRequirement = 'APPLICATION_REQUIREMENT',
  ApplicationDocument = 'APPLICATION_DOCUMENT',
  ApplicationDocumentVersion = 'APPLICATION_DOCUMENT_VERSION',
}
